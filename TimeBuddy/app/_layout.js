import { Stack } from "expo-router";

// for pointing homescreen as the main index(entry) point
// export const unstable_settings = {
//   initialRouteName: "",

// };

export default layout = () => {
  return <Stack />;
};
